{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/tools/.*", "dest": "/api/index" },
    { "src": "/health", "dest": "/api/index" },
    { "src": "/(.*)", "dest": "/api/index" }
  ]
}
